# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when run with the -o flag
*.out

# Project-specific build artifacts
kasaserver
cmd/kasaserver/kasaserver

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# Environment variables specific to a project (REMOVE BEFORE COMMITING IF IT CONTAINS SENSITIVE INFO)
.env

# IDE settings folder
.vscode/
.idea/

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
